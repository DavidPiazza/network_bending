# Audio processing dependencies for Stable Audio latent manipulation nodes
torch>=2.0.0
torchaudio>=2.0.0
librosa>=0.10.0
numpy>=1.21.0
scipy>=1.7.0

# Optional dependencies for advanced features
# soundfile>=0.12.0  # For additional audio format support
# audioread>=3.0.0   # For extended audio format compatibility
# resampy>=0.4.0     # For high-quality resampling